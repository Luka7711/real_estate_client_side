import { getUserGeolocation } from "../../utils";
import { defaultUserLocation } from '../../data';


jest.mock("../../utils/index");

it('returns default city and state', async() => {
    // return defailt city and state
});

